[pytest]
pythonpath = .
asyncio_mode = auto
markers =
    eval: scenario tests that call the live LLM (evals/); excluded from the
        default run since they're slow and consume API quota. Run explicitly
        with `pytest -m eval` or `python -m evals.runner`.
addopts = -m "not eval"
